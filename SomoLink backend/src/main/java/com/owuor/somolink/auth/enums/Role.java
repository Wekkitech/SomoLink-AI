package com.owuor.somolink.auth.enums;

public enum Role {
    ISP_ADMIN,      // Full system admin (view all schools, routers, payments)
    SCHOOL_ADMIN,   // Principal-level admin (only school devices & analytics)
    IT_SUPPORT
}
